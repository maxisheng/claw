# MySQL 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=cms

# 或者直接指定 DSN
# MYSQL_DSN=root:password@tcp(localhost:3306)/cms?charset=utf8mb4&parseTime=True&loc=Local

# JWT 密钥（生产环境请修改）
JWT_SECRET=your-secret-key-change-in-production

# 服务器端口
PORT=8080
